spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/shems2
    username: postgres
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true

client:
  origin-url: http://localhost:3000
springdoc.api-docs.path: /api-docs